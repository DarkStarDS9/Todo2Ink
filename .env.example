# Copy to .env (gitignored) and fill in your own values. deploy-testflight.sh sources this
# automatically if present — see docs/testflight.md for where to find these.
TODO2INK_ASC_KEY_ID=
TODO2INK_ASC_ISSUER_ID=
# Optional — only needed if the key isn't at the default ~/.appstoreconnect/private_keys/ path.
#TODO2INK_ASC_KEY_PATH=
